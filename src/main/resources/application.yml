
spring:
  datasource:
    url: jdbc:postgresql://localhost:5431/desafioVotacao?createDatabaseIfNotExist=true
    username: postgres
    password: postgres
  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    generate-ddl: true
    hibernate:
      ddl-auto: update
  data:
    rest:
      default-media-type: application/json